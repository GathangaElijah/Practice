package main

import "fmt"

// In this program, we are working with 2 arrays a and b both have integers.
// We are subtracting a list from the other and returning the results.
// It also removes all elements in a which are also present in b
// If a value in b, all of its occurences are removed from a
func ArrayDiff(a, b []int) []int {

}

func main() {
	arr1 := 
	fmt.Println(ArrayDiff())
}